/**
 * Handles response file parsing.
 */
module gdmd.response;

/**
 * Search for environment variable or file named _name_.
 * Then search for command line arguments in the file or variable
 * and recursively expand further response variables.
 */
string[] parseResponse(string name)
{
    assert(false, "Response file parsing not implemented");
}
